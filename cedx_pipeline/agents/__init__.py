"""Multi-agent fleet sub-package.

Provides the agent contract interface, individual agent implementations
(orchestrator, worker, verifier), observability tracing, model routing,
and result queues.
"""
